name: dirish-tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: shebzul
        shell: bash
        env:
          TEPESI: ${{ secrets.DIRISH }}
          ALNI: ${{ secrets.SHEBZUL }}
        run: |
          echo $ALNI
          #echo $TEPESI | base64 -d

        

