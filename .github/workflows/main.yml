name: dirish-tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: shebzul
        shell: bash
        env:
          TEPESI: ${{ secrets.DIRISH }}
        run: |
          echo $TEPESI | base64 -d

        

